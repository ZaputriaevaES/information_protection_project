# information_protection_project

# 📡 Защита передачи данных с датчиков DHT11

## 📋 Обзор проекта

Проект реализует безопасную передачу данных с датчиков температуры и влажности между двумя платами STM32 с использованием:
- **HMAC-SHA256** для аутентификации сообщений
- **PRESENT-80** для легковесного шифрования
- **ESP-01 (ESP8266)** для беспроводной связи по Wi-Fi
- **7-сегментного индикатора** для отображения данных

## 🎯 Архитектура системы

### 🔵 TX (Передающая сторона)
- **MCU**: STM32
- **Датчик**: DHT11 на PA7
- **Wi-Fi**: ESP-01 на UART2
- **Индикация**: 
  - PC8 (синий) - ошибка
  - PC9 (зеленый) - успешная отправка

### 🔴 RX (Принимающая сторона)
- **MCU**: STM32
- **Wi-Fi**: ESP-01 на UART2
- **Индикатор**: 7-сегментный на порту B
- **Кнопка**: PA0 для переключения режимов
- **Индикация**:
  - PC8 (синий) - ошибка проверки
  - PC9 (зеленый) - успешный прием

### 🔐 Алгоритмы безопасности

#### 1. **HMAC-SHA256**
- Используется для аутентификации сообщений
- Ключ: 32 байта (общий для TX и RX)
- В пакет включаются первые 8 байт хеша

#### 2. **PRESENT-80**
- Легковесный блочный шифр
- Ключ: 80 бит (10 байт)
- Шифруются только поля `temp_c` и `hum_pc`

## 📶 Настройка Wi-Fi

### RX (Точка доступа)
- **SSID**: `STM_RX`
- **Пароль**: `ohd23fkky169sf6`
- **Режим**: AP
- **TCP-сервер**: порт 5000
- **IP-адрес**: `192.168.4.1`

### TX (Клиент)
- Подключается к точке доступа `STM_RX`
- Устанавливает TCP-соединение с `192.168.4.1:5000`
- Отправляет данные каждые 2 секунды

### 📦 Формат передаваемого пакета 
```c
typedef struct {
    uint16_t sof;      // 0xAA55 - начало пакета
    uint8_t  ver;      // 1 - версия протокола
    uint32_t seq;      // порядковый номер
    uint32_t ts_ms;    // временная метка
    int16_t  temp_c;   // температура (целое, °C)
    uint16_t hum_pc;   // влажность (целое, %)
    uint8_t  hmac[8];  // первые 8 байт HMAC-SHA256
    uint16_t eof;      // 0x55AA - конец пакета
} hmac_frame_t;
```

## 📊 Отображение данных происходит на 7-сегментном индикаторе
```
[разряд]  [значение]
    4-3    'tp' или 'hu' (температура/влажность)
    2-1    двузначное число (0-99)
```
- **Кнопка PA0**: переключение между температурой и влажностью
- **Автоматическое обновление**: каждую секунду

---
**Разработчики**:
- **[Запутряева Екатерина](https://github.com/ZaputriaevaES)** - Ведущий разработчик
- **[Кузнецова Елизавета](https://github.com/lizz-kuzz)** - Embedded инженер
- **[Ритерман Алиса](https://github.com/Alibaakaa)** - QA инженер

**Курс**: Защита информации  
**Год**: 2025